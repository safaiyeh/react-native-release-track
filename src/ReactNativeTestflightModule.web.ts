// Web (react-native-web) stub: this native module isn't available on web.
// Exporting `null` makes `requireNativeModule('ReactNativeTestflight')` resolve
// to `null` so that projects using react-native-web compile without runtime errors.

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore – we deliberately export null for unsupported platform
export default null;
